<!DOCTYPE html>
<html>

<head>
    <title>
        Join
    </title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    <% if(!isMember) { %>
        <h1>Enter the secret password to become a member</h1>
        <form action="" method="POST">
            <label for="secret-password">Secret Password</label>
            <input id="secret-password" name="secret-password" type="text" />
            <button>Submit</button>
            <% } %>

                <% if(isMember) { %>
                    <h1>click button to leave group</h1>
                    <form action="" method="POST">
                        <button>Leave Group</button>
                    </form>
                    <% } %>
        </form>
</body>

</html>